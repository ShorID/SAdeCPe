import React from "react";
import PropTypes from "prop-types";
import AdminLayout from "@/components/Layout/AdminLayout";

const Requests = (props) => {
  return <AdminLayout>Requests</AdminLayout>;
};

Requests.propTypes = {};

export default Requests;
