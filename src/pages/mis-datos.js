import React from 'react'
import PropTypes from 'prop-types'
import Layout from '@/components/Layout'

const MyData = props => {
  return (
    <Layout>MyData</Layout>
  )
}

MyData.propTypes = {}

export default MyData